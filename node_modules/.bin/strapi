#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strapi/bin/_spawn.js" "$@"
  ret=$?
else 
  node  "$basedir/../strapi/bin/_spawn.js" "$@"
  ret=$?
fi
exit $ret
